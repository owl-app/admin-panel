export const LIFECYCLE_EVENTS = {
    REQUEST: {
        ON_BEFORE_EACH: 'onBeforeEach',
        ON_AFTER_EACH: 'onAfterEach'
    }
}

export const STORES_INJECT = "stores";
export const API_INJECT = "api";